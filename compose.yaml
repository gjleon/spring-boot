services:
  mysql:
    image: mysql:9.0.1
    environment:
      MYSQL_ROOT_PASSWORD: ${ENV_ROOT_PASSWORD}
      MYSQL_USER: ${ENV_MYSQL_USER}
      MYSQL_USER_PASSWORD: ${ENV_MYSQL_USER_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - gjleon-db:/var/lib/mysql


volumes:
  gjleon-db: